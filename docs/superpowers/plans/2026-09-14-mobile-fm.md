# 移动端文件管理器实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 移动端（≤768px）文件管理器改为「内容全屏 + 侧滑抽屉目录树 + 面包屑 + ⋯ action sheet」，桌面端零改动。

**Architecture:** 纯表现层改造。目录树双份渲染（桌面 aside 常驻不动 + 移动端 `<dialog>` 抽屉内一份，CSS 断点显隐，避免 SSR 断点判断与首帧闪变）；行操作收进通用 `ActionSheet.svelte`（`<dialog>` 原生 top-layer/Esc/焦点管理）；抽屉开时 pushState、系统返回键 popstate 关抽屉；面包屑用新纯函数 `ancestorChainOf`。表单全部复用现有 SvelteKit actions，零后端改动。

**Tech Stack:** SvelteKit（Svelte 5 runes）、现有 vitest/svelte-check、Playwright（会话内冒烟）。

**Spec:** `docs/superpowers/specs/2026-09-14-mobile-fm-design.md`

**验证命令速查：**
- 单测：`bun run test`（vitest，node 运行时）
- 单文件：`bun run test apps/web/tests/folder-tree.test.ts`
- 类型：`bun --filter remote-reader-web check`
- dev：`bun run dev`（http://localhost:5173，被占切 5174）

---

### Task 1: `ancestorChainOf` 纯函数（TDD）

面包屑需要可点击的 `{id, name}` 链；现有 `folderNamesOf` 只返回名字链且语义是「文档的父目录路径」（三处调用 + 测试，不动它）。

**Files:**
- Modify: `apps/web/src/lib/shared/folder-tree.ts`
- Test: `apps/web/tests/folder-tree.test.ts`

- [ ] **Step 1: 写失败测试**

在 `apps/web/tests/folder-tree.test.ts` 末尾追加（import 行同步加 `ancestorChainOf`）：

```typescript
// ===== ancestorChainOf（文件管理器面包屑，spec 2026-09-14 §5） =====

test('ancestorChainOf 返回自顶向下 id+name 链（含自身）', () => {
    const byId = new Map(fixture().map((x) => [x.id, x]));
    expect(ancestorChainOf(byId, 'd')).toEqual([
        { id: 'a', name: 'a' }, { id: 'b', name: 'b' }, { id: 'c', name: 'c' }, { id: 'd', name: 'd' }
    ]);
});

test('ancestorChainOf 顶层目录返回仅自身', () => {
    const byId = new Map(fixture().map((x) => [x.id, x]));
    expect(ancestorChainOf(byId, 'a')).toEqual([{ id: 'a', name: 'a' }]);
});

test('ancestorChainOf 不存在的 id / 空 Map 返回空数组', () => {
    expect(ancestorChainOf(new Map(), 'nope')).toEqual([]);
});

test('ancestorChainOf 父缺失即止（脏数据安全）', () => {
    const byId = new Map([f('orphan', 'missing')].map((x) => [x.id, x]));
    expect(ancestorChainOf(byId, 'orphan')).toEqual([{ id: 'orphan', name: 'orphan' }]);
});

test('ancestorChainOf parentId 环不死循环（深度上限截断）', () => {
    const cyc = new Map([f('x', 'y'), f('y', 'x')].map((x) => [x.id, x]));
    expect(() => ancestorChainOf(cyc, 'x')).not.toThrow();
    expect(ancestorChainOf(cyc, 'x').length).toBeLessThanOrEqual(1001);
});
```

import 行改为：

```typescript
import { ancestorsOf, visibleNodes, folderNamesOf, ancestorChainOf, type TreeFolder } from '../src/lib/shared/folder-tree';
```

- [ ] **Step 2: 跑测试确认失败**

Run: `bun run test apps/web/tests/folder-tree.test.ts`
Expected: FAIL（`ancestorChainOf` 未导出）

- [ ] **Step 3: 实现**

在 `apps/web/src/lib/shared/folder-tree.ts` 的 `folderNamesOf` 之后追加：

```typescript
// 面包屑用：目录自身的祖先链（自顶向下、含自身，带 id 供点击导航）；
// 父缺失即止（脏数据安全），环走 MAX_TREE_DEPTH 上限安全截断。
export type Crumb = { id: string; name: string };
export function ancestorChainOf(byId: Map<string, TreeFolder>, id: string): Crumb[] {
    const out: Crumb[] = [];
    let cursor: string | null = id;
    let depth = 0;
    while (cursor) {
        const node = byId.get(cursor);
        if (!node) break;
        if (depth++ > MAX_TREE_DEPTH) break;
        out.unshift({ id: node.id, name: node.name });
        cursor = node.parentId;
    }
    return out;
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `bun run test apps/web/tests/folder-tree.test.ts`
Expected: PASS（全部，含原有 13 个）

- [ ] **Step 5: Commit**

```bash
git add apps/web/src/lib/shared/folder-tree.ts apps/web/tests/folder-tree.test.ts
git commit -m "feat(web): folder-tree 新增 ancestorChainOf——面包屑用自顶向下含自身的 id+name 链"
```

---

### Task 2: theme.css 新增 `--rr-scrim`

**Files:**
- Modify: `apps/web/src/styles/theme.css`

- [ ] **Step 1: 加变量**

`:root` 块（`--rr-warning-border` 行后）追加：

```css
    --rr-scrim: rgba(0, 0, 0, 0.4);
```

`[data-theme="dark"]` 块（`--rr-warning-border` 行后）追加：

```css
    --rr-scrim: rgba(0, 0, 0, 0.6);
```

- [ ] **Step 2: Commit**

```bash
git add apps/web/src/styles/theme.css
git commit -m "feat(web): theme.css 新增 --rr-scrim 遮罩语义色（浅 0.4 / 深 0.6）"
```

---

### Task 3: `ActionSheet.svelte` 通用组件

**Files:**
- Create: `apps/web/src/lib/components/ActionSheet.svelte`

无组件测试基建（项目测试全在服务端/纯函数层），验证 = svelte-check + Task 9 Playwright。

- [ ] **Step 1: 写组件**

```svelte
<script lang="ts">
    // 底部 action sheet（spec 2026-09-14 §7.1）：<dialog> 原生 top-layer/Esc/焦点归还。
    // open()/close() 经 bind:this 调用；选项回调 onSelect(key)。打开时锁 body 滚动。
    let {
        label = '操作',
        actions,
        onSelect
    }: {
        label?: string;
        actions: { key: string; label: string; danger?: boolean }[];
        onSelect: (key: string) => void;
    } = $props();

    let dialog = $state<HTMLDialogElement | null>(null);
    let open = $state(false);

    export function show(): void {
        dialog?.showModal();
    }
    export function hide(): void {
        dialog?.close();
    }

    function onDialogClose(): void {
        open = false;
        document.body.style.overflow = '';
    }

    function pick(key: string): void {
        hide();
        onSelect(key);
    }

    $effect(() => {
        if (open) document.body.style.overflow = 'hidden';
    });
</script>

<svelte:window onkeydown={(e) => { if (e.key === 'Escape' && dialog?.open) hide(); }} />

<dialog
    class="sheet"
    aria-label={label}
    bind:this={dialog}
    onclose={onDialogClose}
>
    <ul class="sheet-list">
        {#each actions as a (a.key)}
            <li>
                <button type="button" class="action" class:danger={a.danger} onclick={() => pick(a.key)}>
                    {a.label}
                </button>
            </li>
        {/each}
    </ul>
    <button type="button" class="action cancel" onclick={hide}>取消</button>
</dialog>

<style>
    .sheet {
        position: fixed; inset: auto 0 0 0;
        width: 100%; max-width: 30rem; margin: 0 auto;
        border: none; border-radius: 12px 12px 0 0;
        padding: 0.5rem 0.75rem calc(0.75rem + env(safe-area-inset-bottom, 0px));
        background: var(--rr-card-bg); color: var(--rr-text);
        font-family: system-ui, sans-serif;
    }
    .sheet::backdrop { background: var(--rr-scrim); }
    .sheet[open] { animation: sheet-in 180ms ease-out; }
    @keyframes sheet-in { from { transform: translateY(100%); } }

    .sheet-list { list-style: none; margin: 0; padding: 0; }
    .action {
        display: block; width: 100%; min-height: 48px;
        border: none; background: none; cursor: pointer;
        padding: 0.75rem 0.5rem; font-size: 1rem; text-align: center;
        border-radius: 8px; color: var(--rr-text);
    }
    .action:hover { background: var(--rr-hover-bg); }
    .action:active { background: var(--rr-hover-bg); }
    .action:focus-visible { outline: 2px solid var(--rr-accent); outline-offset: -2px; }
    .action.danger { color: var(--rr-danger); }
    .sheet-list li + li .action { border-top: 1px solid var(--rr-border-soft); }
    .cancel { margin-top: 0.5rem; font-weight: 600; border-top: 1px solid var(--rr-border); border-radius: 8px; }
</style>
```

注意：`<dialog>` 原生 Esc 触发 `cancel`→`close`，`onclose` 统一解锁滚动；svelte:window 的 keydown 是冗余双保险（原生已处理），可省——若 svelte-check 报未用变量则删掉该行。

- [ ] **Step 2: svelte-check**

Run: `bun --filter remote-reader-web check`
Expected: 0 errors（若 `open` state 报「未读取」，把 `$effect` 中的锁滚动逻辑移入 `show()` 同步执行：`document.body.style.overflow = 'hidden'`，`$effect` 与 `open` state 删除，`onDialogClose` 里解锁。二选一，以 check 通过为准）

- [ ] **Step 3: Commit**

```bash
git add apps/web/src/lib/components/ActionSheet.svelte
git commit -m "feat(web): ActionSheet 通用底部操作菜单组件——dialog top-layer + 48px 触屏命中区"
```

---

### Task 4: `+page.svelte` 头部重构（汉堡 + 面包屑 + ＋📁 展开）

**Files:**
- Modify: `apps/web/src/routes/+page.svelte`

本 Task 只做头部；抽屉在 Task 6、行操作在 Task 5。`openDrawer` 先声明占位（Task 6 实现 dialog 前按钮暂时无操作不算破坏——同一 PR 内完成）。

- [ ] **Step 1: script 区新增状态与导入**

import 区加：

```typescript
    import { ancestorChainOf } from '$lib/shared/folder-tree';
```

state 区加：

```typescript
    let showCreate = $state(false);
    let drawerOpen = $state(false);
    let drawerRef = $state<HTMLDialogElement | null>(null);
    let menuBtn = $state<HTMLButtonElement | null>(null);
```

`folderById` 派生下加：

```typescript
    const crumbs = $derived(view === 'dir' && currentDir ? ancestorChainOf(folderById, currentDir) : []);
```

- [ ] **Step 2: 替换 `fm-head` 模板**

现有 `<div class="fm-head">…</div>` 整块（h1 + segmented + create-folder form）替换为：

```svelte
        <div class="fm-head">
            <div class="fm-title">
                <button type="button" class="icon-btn menu-btn mobile-only" bind:this={menuBtn}
                    aria-label="打开目录树" onclick={openDrawer}>
                    <svg viewBox="0 0 16 16" width="18" height="18" fill="currentColor" aria-hidden="true"><path d="M1 2.75A.75.75 0 0 1 1.75 2h12.5a.75.75 0 0 1 0 1.5H1.75A.75.75 0 0 1 1 2.75Zm0 5A.75.75 0 0 1 1.75 7h12.5a.75.75 0 0 1 0 1.5H1.75A.75.75 0 0 1 1 7.75ZM1.75 12h12.5a.75.75 0 0 1 0 1.5H1.75a.75.75 0 0 1 0-1.5Z"></path></svg>
                </button>
                {#if view === 'dir'}
                    <nav class="crumbs" aria-label="当前目录路径">
                        <button type="button" class="crumb" class:current={crumbs.length === 0}
                            onclick={() => selectDir(null)}>根目录</button>
                        {#each crumbs as c, i}
                            <span class="crumb-sep" aria-hidden="true">/</span>
                            {#if i === crumbs.length - 1}
                                <span class="crumb current" aria-current="page">{c.name}</span>
                            {:else}
                                <button type="button" class="crumb" onclick={() => selectDir(c.id)}>{c.name}</button>
                            {/if}
                        {/each}
                    </nav>
                {:else}
                    <h1>{view === 'recent' ? '最近文档' : '最近浏览'}</h1>
                {/if}
            </div>
            <div class="fm-title">
                <div class="segmented" role="group" aria-label="视图切换">
                    <button type="button" class="seg-btn" class:active={view === 'dir'}
                        aria-pressed={view === 'dir'} onclick={() => switchView('/')}>目录内容</button>
                    <button type="button" class="seg-btn" class:active={view === 'recent'}
                        aria-pressed={view === 'recent'} onclick={() => switchView('/?view=recent')}>最近文档</button>
                    <button type="button" class="seg-btn" class:active={view === 'viewed'}
                        aria-pressed={view === 'viewed'} onclick={() => switchView('/?view=viewed')}>最近浏览</button>
                </div>
                {#if view === 'dir'}
                    <button type="button" class="btn sm mobile-only" onclick={() => (showCreate = !showCreate)}>＋ 文件夹</button>
                {/if}
            </div>
            {#if view === 'dir'}
                <form class="create-folder desktop-only" method="POST" action="?/createFolder"
                    use:enhance={() => async ({ result }) => { if (result.type === 'success') await invalidateAll(); }}>
                    <input name="name" placeholder="新文件夹名" required>
                    <button class="btn primary" type="submit">+ 新建文件夹</button>
                </form>
            {/if}
            {#if showCreate && view === 'dir'}
                <form class="create-folder mobile-only" method="POST" action="?/createFolder"
                    use:enhance={() => async ({ result }) => { if (result.type === 'success') { showCreate = false; await invalidateAll(); } }}>
                    <input name="name" placeholder="新文件夹名" required>
                    <button class="btn primary" type="submit">新建</button>
                    <button type="button" class="btn" onclick={() => (showCreate = false)}>收起</button>
                </form>
            {/if}
        </div>
```

- [ ] **Step 3: 头部相关 CSS（style 区）**

加（现有 `.fm-head h1`/`.fm-title` 规则保留）：

```css
    .crumbs { display: flex; align-items: center; gap: 0.25rem; min-width: 0; overflow-x: auto; scrollbar-width: none; font-size: 1.1rem; }
    .crumbs::-webkit-scrollbar { display: none; }
    .crumb { border: none; background: none; color: var(--rr-link); cursor: pointer; padding: 0.2rem 0.3rem; border-radius: 4px; white-space: nowrap; font-size: inherit; }
    .crumb:hover { background: var(--rr-hover-bg); }
    .crumb.current { color: var(--rr-text); font-weight: 600; white-space: nowrap; padding: 0.2rem 0.3rem; }
    .crumb-sep { color: var(--rr-text-muted); flex-shrink: 0; }
    .menu-btn { padding: 0.45rem 0.5rem; }
```

`@media (max-width: 768px)` 块内加（Task 8 会整体重写该块，此处先保证不破）：

```css
        .fm-head h1 { font-size: 1.1rem; }
```

- [ ] **Step 4: 临时 `openDrawer` 占位（Task 6 实现）**

script 区加（Task 6 替换为真实现）：

```typescript
    function openDrawer(): void { /* Task 6 实现：pushState + showModal */ }
```

- [ ] **Step 5: svelte-check + 测试回归**

Run: `bun --filter remote-reader-web check && bun run test`
Expected: check 0 errors；测试全过（387）

- [ ] **Step 6: Commit**

```bash
git add apps/web/src/routes/+page.svelte
git commit -m "feat(web): 文件管理器头部重构——面包屑替换占位标题 + 移动端汉堡与＋文件夹展开"
```

---

### Task 5: 目录行移动端 `⋯` 菜单（ActionSheet 接线）

**Files:**
- Modify: `apps/web/src/routes/+page.svelte`

- [ ] **Step 1: script 区加状态与动作**

```typescript
    import ActionSheet from '$components/ActionSheet.svelte';
```

state 区加：

```typescript
    let sheet = $state<ActionSheet | null>(null);
    let sheetItem = $state<{ id: string; type: string } | null>(null);
```

函数区加（`startRename` 附近；`doDelete` 走 fetch 直调 `?/delete`，RecentList `submitAction` 同款先例）：

```typescript
    function openSheet(id: string, type: string): void {
        sheetItem = { id, type };
        sheet?.show();
    }

    function onSheetAction(key: string): void {
        const it = sheetItem;
        if (!it) return;
        sheetItem = null;
        if (key === 'rename') startRename(it.id);
        else if (key === 'tags') { taggingId = it.id; tagInput = ''; }
        else if (key === 'move') startMove(it.id);
        else if (key === 'delete') void doDelete(it.id, it.type);
    }

    async function doDelete(id: string, type: string): Promise<void> {
        const msg = type === 'folder'
            ? '确认删除该文件夹？将级联删除其全部内容，且不可恢复。'
            : '确认删除该文件？此操作不可恢复。';
        if (!confirm(msg)) return;
        const fd = new FormData();
        fd.set('id', id);
        const r = await fetch('?/delete', { method: 'POST', body: fd });
        if (r.ok) { await invalidateAll(); await recentRef?.reSync(); }
    }
```

- [ ] **Step 2: 行模板改造**

`.actions` span（目录行非编辑态那段）整体替换：

```svelte
                                <span class="actions">
                                    <button type="button" class="icon-btn more-btn mobile-only" aria-label="更多操作"
                                        onclick={() => openSheet(item.id, item.type)}>
                                        <svg viewBox="0 0 16 16" width="18" height="18" fill="currentColor" aria-hidden="true"><path d="M8 9a1.5 1.5 0 1 0 0-3 1.5 1.5 0 0 0 0 3ZM1.5 9a1.5 1.5 0 1 0 0-3 1.5 1.5 0 0 0 0 3Zm13 0a1.5 1.5 0 1 0 0-3 1.5 1.5 0 0 0 0 3Z"></path></svg>
                                    </button>
                                    <span class="desktop-only inline-actions">
                                        <button class="icon-btn" title="重命名" onclick={() => startRename(item.id)}>✏</button>
                                        {#if movingId === item.id}
                                            <span class="hint">← 左树选目标</span>
                                            <button class="btn sm" onclick={() => (movingId = null)}>取消</button>
                                        {:else}
                                            <button class="icon-btn" title="移动到…" onclick={() => startMove(item.id)}>📂</button>
                                        {/if}
                                        <form class="inline" method="POST" action="?/delete"
                                            use:enhance={({ cancel }) => {
                                                const msg = item.type === 'folder'
                                                    ? '确认删除该文件夹？将级联删除其全部内容，且不可恢复。'
                                                    : '确认删除该文件？此操作不可恢复。';
                                                if (!confirm(msg)) { cancel(); return; }
                                                return async ({ result }) => { if (result.type === 'success') await invalidateAll(); };
                                            }}
                                        >
                                            <input type="hidden" name="id" value={item.id}>
                                            <button class="icon-btn danger" title="删除">🗑</button>
                                        </form>
                                    </span>
                                </span>
```

页面底部（`.fm` div 之后、`<style>` 之前）挂 sheet：

```svelte
<ActionSheet
    bind:this={sheet}
    label="文档操作"
    actions={[
        { key: 'rename', label: '重命名' },
        ...(sheetItem?.type === 'file' ? [{ key: 'tags', label: '编辑标签' }] : []),
        { key: 'move', label: '移动到…' },
        { key: 'delete', label: '删除', danger: true }
    ]}
    onSelect={onSheetAction}
/>
```

- [ ] **Step 3: CSS**

```css
    .more-btn { padding: 0.45rem 0.5rem; }
    .inline-actions { display: inline-flex; align-items: center; gap: 0.2rem; }
```

- [ ] **Step 4: svelte-check + 测试回归**

Run: `bun --filter remote-reader-web check && bun run test`
Expected: 0 errors / 全过

- [ ] **Step 5: Commit**

```bash
git add apps/web/src/routes/+page.svelte
git commit -m "feat(web): 目录行移动端 ⋯ 菜单接 ActionSheet——桌面四按钮原样保留"
```

---

### Task 6: 目录树抽屉（dialog + 返回键 + 移动选择模式）

**Files:**
- Modify: `apps/web/src/routes/+page.svelte`

- [ ] **Step 1: script 区——替换 Task 4 的占位 `openDrawer`，实现完整抽屉逻辑**

```typescript
    // 抽屉（spec §6）：pushState 兜系统返回键；非返回键关闭须 await popstate 消费完
    // 那条 state 再做后续导航（SvelteKit goto 也 pushState，乱序会弹掉刚推的导航条目）。
    function popOnce(): Promise<void> {
        return new Promise((resolve) => {
            const once = () => { window.removeEventListener('popstate', once); resolve(); };
            window.addEventListener('popstate', once);
            history.back();
        });
    }

    function openDrawer(): void {
        drawerOpen = true;
        history.pushState({ rrDrawer: true }, '');
        drawerRef?.showModal();
        document.body.style.overflow = 'hidden';
    }

    async function closeDrawer(viaPopstate = false): Promise<void> {
        if (!drawerOpen) return;
        drawerOpen = false;
        drawerRef?.close();
        document.body.style.overflow = '';
        menuBtn?.focus();
        if (!viaPopstate && history.state?.rrDrawer) await popOnce();
    }

    // dialog 原生 Esc 关闭不经 closeDrawer，onclose 兜底同步状态
    function onDrawerClose(): void {
        if (drawerOpen) void closeDrawer();
    }

    // 抽屉内选目录：先消费完 history 再 goto（见 popOnce 注释）
    async function selectFromDrawer(id: string | null): Promise<void> {
        await closeDrawer();
        await selectDir(id);
    }

    $effect(() => {
        const onPop = () => { if (drawerOpen) void closeDrawer(true); };
        window.addEventListener('popstate', onPop);
        return () => window.removeEventListener('popstate', onPop);
    });
```

`startMove` 改为（移动端自动开抽屉；`selecting` 高亮复用现有逻辑）：

```typescript
    function startMove(id: string) {
        movingId = id; moveError = null;
        if (typeof window !== 'undefined' && window.matchMedia('(max-width: 768px)').matches) openDrawer();
    }
```

`pickTarget` 成功分支加关抽屉（桌面 `drawerOpen` 为 false 时是 no-op）：

```typescript
        if (r.ok) {
            movingId = null;
            moveError = null;
            await closeDrawer();
            await invalidateAll();
            await recentRef?.reSync();
        }
```

- [ ] **Step 2: 模板——`.fm-left` aside 之后（`.fm` div 内或紧随其后均可，放 `.fm` 之后语义更清晰）挂抽屉**

`.fm` div 闭合后、ActionSheet 之前：

```svelte
<dialog class="drawer" bind:this={drawerRef} onclose={onDrawerClose} aria-label="目录导航">
    {#if movingId !== null}
        <p class="hint">选择移动目标，或<button class="link" onclick={() => (movingId = null)}>取消</button></p>
        {#if moveError}<p class="error">{moveError}</p>{/if}
    {/if}
    <FolderTree
        folders={treeFolders}
        currentId={view === 'dir' ? currentDir : undefined}
        selecting={movingId !== null}
        onSelect={movingId !== null ? pickTarget : selectFromDrawer}
        storageKey="rr:tree-expanded:{data.user?.id ?? 'anon'}"
    />
</dialog>
```

（与 aside 内那份同 props——双份渲染靠 CSS 断点显隐，见 Step 3；storageKey 相同，折叠记忆同浏览器跨断点共享。）

- [ ] **Step 3: CSS**

```css
    .drawer {
        position: fixed; inset: 0 auto 0 0;
        width: min(80vw, 20rem); max-width: none; max-height: none;
        margin: 0; border: none; border-right: 1px solid var(--rr-border);
        border-radius: 0; padding: 1rem 0.75rem;
        background: var(--rr-card-bg); color: var(--rr-text);
        font-family: system-ui, sans-serif;
    }
    .drawer::backdrop { background: var(--rr-scrim); }
    .drawer[open] { animation: drawer-in 180ms ease-out; }
    @keyframes drawer-in { from { transform: translateX(-100%); } }
    @media (min-width: 769px) { .drawer { display: none !important; } }
```

- [ ] **Step 4: svelte-check + 测试回归**

Run: `bun --filter remote-reader-web check && bun run test`
Expected: 0 errors / 全过

- [ ] **Step 5: Commit**

```bash
git add apps/web/src/routes/+page.svelte
git commit -m "feat(web): 目录树移动端抽屉——dialog 实现 + pushState 兜返回键 + 移动选择模式自动开启"
```

---

### Task 7: RecentList 移动端 `⋯` 菜单

**Files:**
- Modify: `apps/web/src/lib/components/RecentList.svelte`

- [ ] **Step 1: script 区**

import 加：

```typescript
    import ActionSheet from '$components/ActionSheet.svelte';
```

state 加：

```typescript
    let sheet = $state<ActionSheet | null>(null);
    let sheetId = $state<string | null>(null);
```

函数加（`startRename` 附近）：

```typescript
    function openSheet(id: string): void {
        sheetId = id;
        sheet?.show();
    }

    function onSheetAction(key: string): void {
        const id = sheetId;
        const item = rows.find((x) => x.id === id);
        sheetId = null;
        if (!item) return;
        if (key === 'rename') startRename(item);
        else if (key === 'tags') { taggingId = item.id; tagInput = item.tags.map((t) => t.name).join(', '); }
        else if (key === 'move') onStartMove(item.id);
        else if (key === 'delete') void doDelete(item);
    }
```

- [ ] **Step 2: 行模板 `.actions` span 替换**

```svelte
                    <span class="actions">
                        <button type="button" class="icon-btn more-btn mobile-only" aria-label="更多操作"
                            onclick={() => openSheet(item.id)}>
                            <svg viewBox="0 0 16 16" width="18" height="18" fill="currentColor" aria-hidden="true"><path d="M8 9a1.5 1.5 0 1 0 0-3 1.5 1.5 0 0 0 0 3ZM1.5 9a1.5 1.5 0 1 0 0-3 1.5 1.5 0 0 0 0 3Zm13 0a1.5 1.5 0 1 0 0-3 1.5 1.5 0 0 0 0 3Z"></path></svg>
                        </button>
                        <span class="desktop-only inline-actions">
                            <button class="icon-btn" title="重命名" onclick={() => startRename(item)}>✏</button>
                            {#if movingId === item.id}
                                <span class="hint">← 左树选目标</span>
                                <button class="btn sm" onclick={onCancelMove}>取消</button>
                            {:else}
                                <button class="icon-btn" title="移动到…" onclick={() => onStartMove(item.id)}>📂</button>
                            {/if}
                            <button class="icon-btn danger" title="删除" onclick={() => void doDelete(item)}>🗑</button>
                        </span>
                    </span>
```

组件底部挂：

```svelte
<ActionSheet
    bind:this={sheet}
    label="文档操作"
    actions={[
        { key: 'rename', label: '重命名' },
        { key: 'tags', label: '编辑标签' },
        { key: 'move', label: '移动到…' },
        { key: 'delete', label: '删除', danger: true }
    ]}
    onSelect={onSheetAction}
/>
```

（「编辑标签」入口的 🏷 按钮现状在 `.doc-tags` 里——顺手包 `.desktop-only`，移动端标签编辑统一走菜单。）

- [ ] **Step 3: CSS（style 区加）**

```css
    .more-btn { padding: 0.45rem 0.5rem; }
    .inline-actions { display: inline-flex; align-items: center; gap: 0.2rem; }
```

- [ ] **Step 4: svelte-check + 测试回归**

Run: `bun --filter remote-reader-web check && bun run test`
Expected: 0 errors / 全过

- [ ] **Step 5: Commit**

```bash
git add apps/web/src/lib/components/RecentList.svelte
git commit -m "feat(web): RecentList 行移动端 ⋯ 菜单——标签编辑入口一并收进菜单"
```

---

### Task 8: 移动端布局 CSS 收尾

**Files:**
- Modify: `apps/web/src/routes/+page.svelte`（style 区）
- Modify: `apps/web/src/lib/components/RecentList.svelte`（style 区，仅一处 :active）

- [ ] **Step 1: `+page.svelte` 通用显隐类（style 区顶部，全局两组件都要用——放本页会 scoped！需放全局）**

⚠️ Svelte 样式是 scoped 的，`.mobile-only`/`.desktop-only` 两组件都用——放 `apps/web/src/styles/theme.css` 末尾（全局单源，主题文件已是全局样式载体）：

```css
/* 响应式显隐（spec 2026-09-14 移动端文件管理器）：桌面默认显、移动端藏反之 */
.mobile-only { display: none; }
@media (max-width: 768px) {
    .mobile-only { display: revert; }
    .desktop-only { display: none; }
}
```

注意 `display: revert` 回到元素默认 display（button→inline-block 等）；对 inline 容器 span 已包 flex 类不受影响。

- [ ] **Step 2: 重写 `+page.svelte` 的 `@media (max-width: 768px)` 块**

替换现有移动端块为：

```css
    @media (max-width: 768px) {
        .fm { flex-direction: column; gap: 0; padding: 0.75rem 1rem; height: auto; }
        .fm-left { display: none; }
        .fm-right { overflow-y: visible; }
        .fm-head { gap: 0.6rem; }
        .create-folder.mobile-only { width: 100%; }
        .create-folder.mobile-only input { flex: 1; min-width: 0; }
        .item { padding: 0.5rem 0.25rem; }
        .item:active { background: var(--rr-hover-bg); }
        .crumbs { flex: 1; }
        .menu-btn { flex-shrink: 0; }
    }
```

- [ ] **Step 3: RecentList 触屏反馈**

style 区窄屏块（已有 `@media (max-width: 768px)`）内加一行：

```css
        .item:active { background: var(--rr-hover-bg); }
```

- [ ] **Step 4: svelte-check + 测试回归**

Run: `bun --filter remote-reader-web check && bun run test`
Expected: 0 errors / 全过

- [ ] **Step 5: Commit**

```bash
git add apps/web/src/routes/+page.svelte apps/web/src/styles/theme.css apps/web/src/lib/components/RecentList.svelte
git commit -m "feat(web): 移动端布局收尾——树让位抽屉、单滚动轴、触屏 :active 反馈与显隐断点类"
```

---

### Task 9: 全量验证 + Playwright 冒烟

**Files:** 无新改动（验证任务；发现问题回上游 Task 修）

- [ ] **Step 1: 全量静态验证**

Run: `bun --filter remote-reader-web check && bun --filter remote-reader-mcp-bridge check && bun run test`
Expected: check 双 0 errors；测试全过（387+5=392 左右）

- [ ] **Step 2: 起 dev + seed 数据**

```bash
bun run dev   # 或已知 5174
node scripts/seed-token.mjs <email>   # 若无 token
# 用 e2e 思路造层级数据：curl 上传 path=周报/2026-09 两三个文件
```

- [ ] **Step 3: Playwright 移动 viewport 冒烟（375×667）**

逐项手验/脚本验（会话内 Playwright MCP）：
1. `/` 移动端：树**不占顶部**、内容全宽、单滚动轴
2. 汉堡开抽屉 → 树可见 → 点目录 → 抽屉关、URL 变 `?dir=…`、面包屑更新
3. 抽屉开 → 系统返回键（`page.goBack()`）→ 抽屉关、**页面不退出**（URL 不变）
4. 面包屑中间级点击 → 跳对应目录
5. `⋯` → sheet 弹出 → 重命名（行内表单）/ 删除（confirm+消失）/ 移动到…（抽屉自动开选择模式 → 选目标 → 列表刷新）
6. ＋ 文件夹 → 展开 → 新建成功 → 左树（抽屉里）出现新目录
7. `?view=recent` / `?view=viewed`：标题正常、抽屉可用、行操作同 5

- [ ] **Step 4: 桌面回归（1280×800）**

截图对比：双栏布局、左树常驻、行内四按钮、create-folder 表单——与改造前一致（`.fm-left` 的 DOM/CSS 未动即达标）

- [ ] **Step 5: 双主题抽查**

移动端 + 深色：抽屉、action sheet、面包屑、遮罩色均取语义变量，无写死色

- [ ] **Step 6: 修复回归（如有）并 commit**

```bash
git add -A
git commit -m "fix(web): 移动端文件管理器验收修复——<具体项>"
```

- [ ] **Step 7: 更新 CLAUDE.md 状态段**

在「当前状态」追加一句移动端改造摘要 + 测试数变化，commit：

```bash
git add CLAUDE.md
git commit -m "docs: CLAUDE.md 状态更新——移动端文件管理器交付"
```

---

## Self-Review 记录

- **Spec 覆盖**：§5 头部→Task 4；§6 抽屉→Task 6（含 §6.4 返回键+消费）；§7 行操作→Task 5/7；§8 移动流程→Task 6 startMove/pickTarget；§9 主题→Task 2/8（--rr-scrim + 语义变量）；§10 测试→Task 1 TDD + Task 9；§11 切面一致；§12 备案的风险（dialog 兼容/popstate 交互/面包屑滚动/storageKey 共享）均有对应实现注记。
- **占位符**：Task 4 Step 4 的 `openDrawer` 占位在同一 PR 内被 Task 6 替换，非终态占位。无 TBD。
- **类型一致性**：`ancestorChainOf(byId: Map<string, TreeFolder>, id: string): Crumb[]` 与 Task 4 用法（`folderById` 是 `Map<string, TreeFolder>`）一致；`ActionSheet.show()/hide()` 与 Task 5/7 的 `sheet?.show()` 一致；`closeDrawer(viaPopstate?: boolean)` 各调用点签名一致。
- **已知取舍**（spec §12 + 计划内注记）：树双份渲染换取桌面零改动与 SSR 无闪变；跨断点 resize 的折叠记忆过期覆盖为可忽略 edge。
